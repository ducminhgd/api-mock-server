// Infrastructure layer (ssr only): SQLx repository implementations and config loading.
// Implements traits defined in the application layer.
pub mod auth;
pub mod config;
pub mod db;
pub mod state;
